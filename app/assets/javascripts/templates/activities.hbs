<div class="row">
  <div class="col-md-4">
    <div class="panel panel-info">
      <div class="panel-heading">
        <h3 class="panel-title">Logged Activities</h3>
      </div>
      <div id="recent_activity" class="list-group">
        {{#each controller}}
          {{#link-to "activity" this class="list-group-item"}}{{date this.createdAt}} - {{this.exercise.name}}{{/link-to}}
        {{/each}}
      </div>
    </div>
  </div>

  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
