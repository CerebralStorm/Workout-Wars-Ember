<div class="row">
  <div class="col-md-3">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title">Users</h3>
      </div>
      <div class="list-group">
        {{#each controller}}
          {{#unless isDirty}}
            {{#link-to "user" this class="list-group-item"}}{{email}}{{/link-to}}
          {{/unless}}
        {{else}}
          <a href="#" class="list-group-item">There are no competitions.</a>
        {{/each}}
      </div>
    </div>
  </div>

  <div class="col-md-9">
    {{outlet}}
  </div>
</div>


