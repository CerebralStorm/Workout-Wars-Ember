<div class="panel panel-info">
  <div class="panel-heading">
    <h3 class="panel-title">Information for other Users</h3>
  </div>
  <div class="panel-body">
    <div class="row">
      <div class="col-sm-6">
        <h2 class="featurette-heading">They are your friends. <span class="text-muted">They are your enemies.</span></h2>
          <p class="lead">The goal of the <strong>WorkoutWars</strong> user is to increase thier own personal fitness. But who doesn't love a little friendly competition? See how you stack up against your fellow users by selecting their profile to left.</p>
          <p class="lead text-success">Select a user from the left to view their profile</p>
      </div>

      <div class="col-sm-6">
        <img src="/assets/pushup2.jpg" alt="challenges" class="img-rounded img-responsive">
      </div>
    </div>
  </div>
</div>

<h2 class="featurette-heading">Current User Levels <span class="text-muted">Join the madness</span></h2>

<div class="panel panel-info">
  <table class="table table-hover">
    <thead>
      <tr class="active">
        <th>User</th>
        <th>Picture</th>
        <th>Level</th>
        <th>Experience</th>
      </tr>
    </thead>
    <tbody>
    {{#each user in controller}}
    <tr>
      <td>{{user.handle}}</td>
      <td>{{gravatarUrlSmall user}}</td>
      <td><span class="badge">{{user.level}}</span></td>               
      <td><span class="badge">{{user.experience}}</span></td>
    </tr>
    {{/each}}
    </tbody>
  </table>
</div>


<hr class="featurette-divider">

